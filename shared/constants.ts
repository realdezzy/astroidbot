export const DEFAULTS = {
  pollIntervalSeconds: 60,
  dryRun: true,
  bcryptRounds: 12,
  dustThresholdUsd: 0.50,
  tokenCacheTtlMs: 6 * 60 * 60 * 1000,
  fetchCooldownMs: 5 * 60 * 1000,
  defaultSlippageBps: 100,
  defaultMaxPositionPct: 25,
  defaultRebalanceThreshold: 2,
  dcaIntervalMinutes: 60,
  gridLevels: 5,
  gridSpreadBps: 30,
  feeBps: 30,
  aiRefreshMinutes: 15,
  priceHistoryMaxPoints: 500,
  maxCopiesPerCycle: 3,
  maxSniperAmount: 10,
  defaultPort: 8006,
  jwtExpiry: "15m",
  refreshTokenExpiryDays: 30,
  rateLimitWindowMs: 60_000,
  rateLimitMax: 100,
} as const;
